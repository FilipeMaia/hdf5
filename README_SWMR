How to compile the SMWR hdf5 version


- make a build directory
mkdir build

- run configure with the correct options
./configure --prefix=`pwd`/build --enable-threadsafe --with-pthread=/usr

- compile with multiple threads
make -j 8

- install
make install
